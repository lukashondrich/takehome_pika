[project]
name = "discord-bot"
version = "0.1.0"
description = "Discord companion bot with markdown memory, inner-life ticks, and headless evals."
requires-python = ">=3.11"
dependencies = [
    "discord.py>=2.4",
    "anthropic>=0.40",
    "httpx>=0.27",
    "python-dotenv>=1.0",
]
